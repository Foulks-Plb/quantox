export const LowerCTrim = (value: string) => (value.trim()).toLowerCase();
export const fixed2 = (value: number) => (Number(value?.toFixed(2) || 0));
export const fixed4 = (value: number) => (Number(value?.toFixed(4) || 0));